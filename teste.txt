Pesquisa rápida: inserir campo de busca no topo do ListBox para filtrar downloads por nome/URL e reduzir rolagens em listas grandes.
main.rsLines 627-665
let list_box = ListBox::builder()    .selection_mode(gtk4::SelectionMode::None)    .css_classes(vec!["boxed-list"])    .build();
Agrupamento por status: permitir alternar entre seções (ex.: abas ou drop-down) organizadas por DownloadStatus, mantendo métricas e cards consistentes.
main.rsLines 758-857
metrics_panel.append(&metrics_title);metrics_panel.append(&metrics_grid);list_container.append(&metrics_panel);list_container.append(&list_box);
Notificação do sistema: disparar gio::Notification quando um download concluir ou falhar, aproveitando o ToastOverlay apenas como feedback secundário.
main.rsLines 328-355
let toast_overlay = libadwaita::ToastOverlay::new();// ... existing code ...app.add_action(&config_action);
Arrastar e soltar URLs/arquivos: habilitar DnD no main_box para capturar links e listas .txt, disparando add_download automaticamente.
main.rsLines 331-474
let main_box = GtkBox::new(Orientation::Vertical, 0);// ... existing code ...glib::timeout_add_seconds_local(2, {
Detalhes em linha: transformar o botão de informações em um popover expansível dentro do card, exibindo URL, histórico de tentativas e hash sem bloquear a UI principal.